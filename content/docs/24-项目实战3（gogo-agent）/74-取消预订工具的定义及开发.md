---
title: "✅取消预订工具的定义及开发"
---

# ✅取消预订工具的定义及开发

我们用**BookingAgent**（预订执行智能体）完成用户确认方案后的实际预订（机票/酒店/火车票下单）。所有下单结果由 `BookingPersistenceHook` 自动落库到 `booking_record` 表，形成用户的"外部预订记录"。

当用户提出"帮我取消那张去上海的机票"这类需求时，Agent 需要一个**安全、可控**的取消能力，这就是 `cancel_booking` 工具的定位：

| 能力 | 说明 |
| --- | --- |
| 取消范围 | 机票、火车票、酒店三类预订记录 |
| 平台联动 | 机票/火车票：调途牛平台真实取消接口；酒店：暂无线上取消通道，仅标记内部状态 |
| 安全保障 | 订单归属校验（租户隔离）、幂等处理、平台失败时内部状态不变更 |
| 状态同步 | 取消成功后更新`booking_record`状态为`CANCELLED`，取消原因记入`remark` |

取消动作封装为 `cancel_booking` 工具：**LLM 只负责"选对订单、传对参数"，校验、平台调用、状态同步全部在工具内确定性完成**。

```text
用户："取消我的机票订单"
        │
        ▼
   BookingAgent (ReActAgent)
        │  1. query_booking_record 查询/定位订单（BookingReadTools）
        │  2. 与用户确认要取消的具体订单
        ▼
   cancel_booking 工具 (BookingWriteTools)          ←──── 本文主角
        │
        ├─ ① 参数校验（booking_id 非空）
        ├─ ② 查询 BookingRecord，校验 userId 归属（租户隔离）
        ├─ ③ 幂等检查（已 CANCELLED 直接返回成功）
        ├─ ④ cancelOnPlatform：按业务类型分派
        │       ├─ FLIGHT/TRAIN → invokeTuniuCancel（ProcessBuilder 调 tuniu CLI）
        │       └─ HOTEL / 无外部单号 → skip（仅标记内部状态）
        ├─ ⑤ 更新 BookingRecord.status = CANCELLED，reason → remark
        └─ ⑥ 返回 JSON 结果给 LLM
```

工具定义：

```java
@Component
public class BookingWriteTools {

    private static final int CLI_TIMEOUT_SECONDS = 30;
    private static final String TUNIU_PROVIDER = "tuniu-cli";

    /** bizType -> tuniu-cli 子命令映射 */
    private static final Map<BookingType, String> CANCEL_COMMAND_MAP = Map.of(
            BookingType.FLIGHT, "call flight cancelOrder",
            BookingType.TRAIN, "call train cancelOrder"
    );

    private final BookingRecordRepository bookingRecordRepository;
    private final ApiKeyService apiKeyService;

    @Tool(name = "cancel_booking",
            description = "取消用户的一条外部预订记录。机票和火车票会同步调用平台取消接口；"
                          + "酒店目前无线上取消通道，仅标记内部状态为已取消并提示用户联系酒店。"
                          + "已取消的记录重复调用会直接返回成功（幂等）。")
    public String cancelBooking(
            AgentSessionContext sessionCtx,
            @ToolParam(name = "booking_id", description = "要取消的内部预订单号（如 bk_xxx）") String bookingId,
            @ToolParam(name = "reason", description = "取消原因，可选", required = false) String reason) {
        // ...
    }
}
```

核心流程

```java
public String cancelBooking(AgentSessionContext sessionCtx, String bookingId, String reason) {
    String userId = sessionCtx.getUserId();

    // 0. 参数校验
    if (bookingId == null || bookingId.isBlank()) {
        return errorResponse("INVALID_PARAM", "booking_id 不能为空");
    }

    // 1. 查询并校验归属（租户隔离）
    BookingRecord record = bookingRecordRepository.findByBookingId(bookingId).orElse(null);
    if (record == null) {
        return errorResponse("BOOKING_NOT_FOUND", "预订记录不存在：" + bookingId);
    }
    if (!userId.equals(record.getUserId())) {
        return errorResponse("PERMISSION_DENIED", "无权操作该预订记录，归属用户不匹配。");
    }

    // 2. 幂等：已取消的直接返回成功
    if (record.getStatus() == BookingStatus.CANCELLED) {
        return successResult(bookingId, record.getBizType(), true, "该预订记录已处于取消状态，无需重复取消。");
    }

    // 3. 按业务类型执行平台取消
    PlatformCancelOutcome outcome = cancelOnPlatform(bizType, record.getExternalOrderNo(), userId, bookingId);
    if (!outcome.proceed) {
        return errorResponse("PLATFORM_CANCEL_FAILED", "外部平台取消失败，内部预订状态未变更。原因：" + outcome.message);
    }

    // 4. 平台侧成功（或 skip）后才更新内部状态
    record.setStatus(BookingStatus.CANCELLED);
    if (reason != null && !reason.isBlank()) {
        record.setRemark(reason);
    }
    bookingRecordRepository.save(record);

    return successResult(bookingId, bizType, outcome.platformCancelled, "预订已取消。" + outcome.message);
}
```

- **先平台、后内部**：内部状态只在平台取消成功（或明确无需平台）后才变更，避免"内部已取消、平台仍有效"的不一致；
- **失败即中止**：平台取消失败时原状态不动，用户可重试，LLM 可据 `message` 向用户解释原因；
- **幂等返回成功而非报错**：重复取消是用户的常见操作（不确定是否成功而重试），返回成功 + 提示语体验更好。

第三方平台取消

```java
private PlatformCancelOutcome cancelOnPlatform(BookingType bizType, String externalOrderNo,
                                               String userId, String bookingId) {
    // ① 不在 CANCEL_COMMAND_MAP 中的类型（酒店等）→ skip，仅标记内部状态
    if (!CANCEL_COMMAND_MAP.containsKey(bizType)) {
        String msg = (bizType == BookingType.HOTEL)
                ? "酒店预订暂不支持线上自动取消，已标记内部状态为已取消，请提示用户联系酒店前台处理。"
                : "该业务类型暂不支持自动取消，已标记内部状态为已取消。";
        return PlatformCancelOutcome.skip(msg);
    }

    // ② 无外部单号（例如下单失败的残留记录）→ skip
    if (externalOrderNo == null || externalOrderNo.isBlank()) {
        return PlatformCancelOutcome.skip("（无外部平台订单号，仅标记内部状态为已取消）");
    }

    // ③ 机票/火车票 → 真实调用平台取消
    CliResult result = invokeTuniuCancel(CANCEL_COMMAND_MAP.get(bizType), externalOrderNo, userId);
    if (result.success()) {
        return PlatformCancelOutcome.cancelled("平台取消请求已发送。");
    }
    return PlatformCancelOutcome.failed(result.errorMsg());
}
```

CLI调用

```java
private CliResult invokeTuniuCancel(String subCommand, String externalOrderNo, String userId) {
    // ① 取用户级途牛 API Key（ApiKeyService，存储于 user_api_key 表）
    String apiKey = apiKeyService.getApiKey(userId, TUNIU_PROVIDER);
    if (apiKey == null || apiKey.isBlank()) {
        return CliResult.fail("用户未配置途牛 API Key，无法调用平台取消接口。");
    }

    // ② 与 TuniuApiKeyHook 保持一致：用 env 前缀注入 API Key
    String cmd = "env TUNIU_API_KEY=%s tuniu %s -a '{\"orderId\":\"%s\"}'"
            .formatted(apiKey, subCommand, externalOrderNo);

    try {
        // ③ 执行命令，合并 stderr 到 stdout 便于排查
        ProcessBuilder pb = new ProcessBuilder("sh", "-c", cmd).redirectErrorStream(true);
        Process process = pb.start();

        String output;
        try (var reader = new BufferedReader(
                new InputStreamReader(process.getInputStream(), StandardCharsets.UTF_8))) {
            output = reader.lines().reduce("", (a, b) -> a + b + "\n").trim();
        }

        // ④ 30 秒超时强制终止，防止进程悬挂阻塞 Agent
        if (!process.waitFor(CLI_TIMEOUT_SECONDS, TimeUnit.SECONDS)) {
            process.destroyForcibly();
            return CliResult.fail("平台取消请求超时，请稍后重试。");
        }
        if (process.exitValue() != 0) {
            return CliResult.fail("平台接口调用异常（exitCode=" + process.exitValue() + "）");
        }

        return parseCliResponse(output);
    } catch (Exception e) {
        return CliResult.fail("平台取消接口调用异常：" + e.getMessage());
    }
}
```

- **用户级凭证**：API Key 按用户隔离（`ApiKeyService.getApiKey(userId, "tuniu-cli")`），未配置时给出可操作的错误提示，LLM 可引导用户先配置；
- **env 前缀注入**：与 `TuniuApiKeyHook` 的注入方式保持一致，避免环境变量残留；
- **超时兜底**：`waitFor(30s)` + `destroyForcibly()`，工具是同步阻塞执行，必须有超时保护，否则会拖死整个 ReAct 循环；
- **异常全捕获**：任何异常都转为 `CliResult.fail`，绝不把异常抛给框架导致 Agent 中断。

响应解析

```java
/**
 * 解析平台响应 JSON。
 * 机票成功：{"successCode": true, "msg": "订单取消成功！", "errorCode": 170000}
 * 火车票成功：{"successCode": true, "errorMessage": null}
 */
CliResult parseCliResponse(String output) {
    if (output == null || output.isBlank()) {
        return CliResult.fail("平台返回空响应");
    }
    // ① CLI 输出可能带日志前缀，截取首个 '{' 到末个 '}' 之间的 JSON 主体
    int start = output.indexOf('{');
    int end = output.lastIndexOf('}');
    if (start < 0 || end <= start) {
        return CliResult.fail("平台响应格式异常");
    }
    try {
        JSONObject json = JSON.parseObject(output.substring(start, end + 1));
        if (Boolean.TRUE.equals(json.getBoolean("successCode"))) {
            return CliResult.ok(output);
        }
        // ② 失败信息：优先 msg，其次 errorMessage（两种业务返回字段名不同）
        String errorMsg = Stream.of(json.getString("msg"), json.getString("errorMessage"))
                .filter(s -> s != null && !s.isBlank())
                .findFirst()
                .orElse("平台返回取消失败（未提供具体原因）");
        return CliResult.fail(errorMsg);
    } catch (Exception e) {
        return CliResult.fail("平台响应解析失败");
    }
}
```

工具注册

```java
@Bean(name = "bookingAgent")
@Scope("prototype")
public ReActAgent build() {
    Toolkit toolkit = new Toolkit(ToolkitConfig.builder().parallel(true).build());
    // 取消预订（cancel_booking）
    toolkit.registration().tool(bookingWriteTools).apply();     
    
    // ... SkillBox、记忆、Hook、系统提示词、maxIters 等装配
}
```

提示词约束

booking-agent-system.md

```markdown
## 取消预订流程
1. 先调用 `query_booking_record` 查询用户的预订记录，定位要取消的订单。
2. 确认用户要取消的具体订单（若有多条则请用户明确指定）。
3. 调用 `cancel_booking` 工具执行取消。
4. 向用户确认取消结果。

取消注意事项：
- **取消只能走 `cancel_booking` 工具**：禁止绕过工具直接用 shell 执行 `cancelOrder`
  类命令——工具内含订单归属校验、取消原因记录与内部状态同步，直连命令没有这些保障。
- **内外单号映射**：`query_booking_record` 返回两套单号——内部预订单号（`bookingId`，
  形如 `bk_xxx`）与外部平台单号（`externalOrderNo`，形如 `TNxxx`）。展示给用户的
  "订单号"一律用外部单号；调用 `cancel_booking` 必须传内部单号，由你完成两者映射，
  不要把外部单号传给取消工具；用户报外部单号时先用 `query_booking_record` 反查对应的
  内部单号。
```

---

来源: https://thoughts.aliyun.com/workspaces/6963289eb0fc2e001bb052eb/docs/6a919538a7c8ff00013f9030
